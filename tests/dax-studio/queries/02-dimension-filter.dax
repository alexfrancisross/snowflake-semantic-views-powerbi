// Dimension filter: WHERE clause on a text dimension.
EVALUATE
SUMMARIZECOLUMNS(
    SV_REGIONAL_SALES[NATION_NAME],
    FILTER(ALL(SV_REGIONAL_SALES[REGION_NAME]), SV_REGIONAL_SALES[REGION_NAME] = "EUROPE"),
    "Total Revenue", SUM(SV_REGIONAL_SALES[TOTAL_REVENUE])
)
