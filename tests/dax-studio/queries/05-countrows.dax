// v3.3.2 regression: COUNTROWS must render as COUNT("<col>"), not COUNT(*).
EVALUATE
SUMMARIZECOLUMNS(
    SV_REGIONAL_SALES[REGION_NAME],
    "Row Count", COUNTROWS(SV_REGIONAL_SALES)
)
ORDER BY SV_REGIONAL_SALES[REGION_NAME]
