// Multi-column ORDER BY across a dimension and a metric.
EVALUATE
SUMMARIZECOLUMNS(
    SV_CUSTOMER_ORDERS[MARKET_SEGMENT],
    SV_CUSTOMER_ORDERS[REGION_NAME],
    "Total Revenue", SUM(SV_CUSTOMER_ORDERS[TOTAL_REVENUE])
)
ORDER BY SV_CUSTOMER_ORDERS[MARKET_SEGMENT], [Total Revenue] DESC
